# 失效状态

下面是一个示例，展示了如何让元素失效

<iframe
  src='https://pf566g-8000.csb.app'
  style={{
    width: "100%",
    height: "500px",
    marginTop: "24px",
    border: "0",
    borderRadius: "4px",
    overflow: "hidden",
  }}
  title='iCraft Player - disabled'
/>

[![Edit disabled](https://codesandbox.io/static/img/play-codesandbox.svg)](https://codesandbox.io/p/devbox/boring-danilo-pf566g?embed=1&workspaceId=22bc0932-d914-4853-b55d-f94999d3fc25&file=%2Fsrc%2Fpages%2Findex.tsx)

```tsx
import type { ClickParams, Element3D, ICraftPlayerInstance } from "@icraft/player";
import { ICraftPlayer } from "@icraft/player";
import { useCallback, useLayoutEffect, useRef, useState } from "react";

export default () => {
  const instanceRef = useRef<ICraftPlayerInstance>();

  const onClick = useCallback((params: ClickParams) => {
    const { item } = params;
    item?.setDisabled(true);
  }, []);

  return (
    <ICraftPlayer
      fileUrl='/AI.iplayer'
      onClick={onClick}
      ref={instanceRef}
      header={false}
    />
  );
};
```
